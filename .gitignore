# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit. Use .env.example as the committed template.
.env
.env.*
!.env.example
!.env.test.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

playwright-report/
test-results/

# Lint output artifacts — never commit these (they're regenerated locally)
lint_results*.txt
new_lint_results*.txt

# Supabase CLI session cache — local-only, drifts per worktree
supabase/.temp/
.zaude/e2e-test/
